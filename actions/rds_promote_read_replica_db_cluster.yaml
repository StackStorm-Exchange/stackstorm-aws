---
description: 
pack: aws
enabled: true
entry_point: run.py
name: rds_promote_read_replica_db_cluster
runner_type: python-script
parameters:
  account_id:
    type: string
  region:
    type: string
  action:
    default: promote_read_replica_db_cluster
    immutable: true
    type: string
  cls:
    default: rds
    type: string
  headers:
    type: string
  module_path:
    default: boto3
    immutable: true
    type: string
  DBClusterIdentifier:
    type: string
    description: "The identifier of the DB cluster Read Replica to promote. This parameter is not case-sensitive.  Constraints:   Must contain from 1 to 63 alphanumeric characters or hyphens.   First character must be a letter.   Cannot end with a hyphen or contain two consecutive hyphens.   Example: my-cluster-replica1 "
    required: true