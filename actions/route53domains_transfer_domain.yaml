description: 
pack: aws
enabled: true
entry_point: run.py
name: route53domains_transfer_domain
runner_type: python-script
parameters:
  action:
    default: transfer_domain
    immutable: true
    type: string
  cls:
    default: route53domains
    type: string
  headers:
    type: string
  module_path:
    default: boto3
    immutable: true
    type: string
  DomainName:
    type: string
    description: "The name of the domain that you want to transfer to Amazon Route 53. Constraints: The domain name can contain only the letters a through z, the numbers 0 through 9, and hyphen (-). Internationalized Domain Names are not supported."
    required: true
  DurationInYears:
    type: integer
    description: "The number of years that you want to register the domain for. Domains are registered for a minimum of one year. The maximum period depends on the top-level domain. Default: 1"
    required: true
  AdminContact:
    type: object
    description: "Provides detailed contact information."
    required: true
  RegistrantContact:
    type: object
    description: "Provides detailed contact information."
    required: true
  TechContact:
    type: object
    description: "Provides detailed contact information."
    required: true
  IdnLangCode:
    type: string
    description: "Reserved for future use."
  Nameservers:
    type: array
    description: "Contains details for the host and glue IP addresses."
  AuthCode:
    type: string
    description: "The authorization code for the domain. You get this value from the current registrar."
  AutoRenew:
    type: boolean
    description: "Indicates whether the domain will be automatically renewed (true) or not (false). Autorenewal only takes effect after the account is charged. Default: true"
  PrivacyProtectAdminContact:
    type: boolean
    description: "Whether you want to conceal contact information from WHOIS queries. If you specify true, WHOIS ('who is') queries will return contact information for our registrar partner, Gandi, instead of the contact information that you enter. Default: true "
  PrivacyProtectRegistrantContact:
    type: boolean
    description: "Whether you want to conceal contact information from WHOIS queries. If you specify true, WHOIS ('who is') queries will return contact information for our registrar partner, Gandi, instead of the contact information that you enter. Default: true "
  PrivacyProtectTechContact:
    type: boolean
    description: "Whether you want to conceal contact information from WHOIS queries. If you specify true, WHOIS ('who is') queries will return contact information for our registrar partner, Gandi, instead of the contact information that you enter. Default: true "